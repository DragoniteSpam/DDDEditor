/// void sme_drag_rectangle(Selection, x, y);
// MAYBE if there's an object under the cursor you want
// to expand the selection on the x axis too, but deal with
// that later.

argument0.x2=argument1;
argument0.y2=argument2;
