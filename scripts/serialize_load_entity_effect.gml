/// void serialize_load_entity_effect(buffer, Entity, version);

serialize_load_entity(argument0, argument1, argument2);

// remember to add collision information - probably in the form of
// a block or sphere
