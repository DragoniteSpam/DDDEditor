/// void ui_render_image_button_tex(UIImageButton, x, y);

argument0.image=get_active_tileset().master;
        debug(string(get_active_tileset().master)+" in ui")

ui_render_image_button(argument0, argument1, argument2);
