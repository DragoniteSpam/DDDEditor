/// uivc_bitfield_tile_passability_passable(UIThing);

// value^1111 won't do what i want it to do
ActiveMap.tileset.passage[# Camera.selection_fill_tile_x, Camera.selection_fill_tile_y]=TILE_PASSABLE;
